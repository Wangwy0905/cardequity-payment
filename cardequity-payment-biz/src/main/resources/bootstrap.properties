# Spring
spring.application.name=cardequity-payment
# Info
info.app.id=${spring.application.name}
info.app.description=cardequity-payment
# Apollo
app.id=${spring.application.name}
apollo.bootstrap.enabled=true
apollo.bootstrap.namespaces=application,YOUYU.dcos-eureka-network,YOUYU.mybatis-mysql-config,YOUYU.monitor
# Mybatis config
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.config-location=classpath:mybatis-config.xml
